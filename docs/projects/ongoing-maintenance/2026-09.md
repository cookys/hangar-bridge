# Ongoing maintenance — 2026-09

| date | commit | fix |
|---|---|---|
| 09-12 | 56c2884 | fix(relay): /v1/messages rejected the reserved `~cli` caller identity → replies to any shell send fanned out to a never-subscribed ULID (matched:0, silently lost); one caller-identity parser shared by messages+replies, mailbox branch now reachable (跨 relay + dotfiles CLI) |
