# relay follow-ups — deploy record (2026-09-17, hub = openclaw)

Candidate `develop@fb4c156eb8d4ddaf7116d11c1675590265d6db2e` (merge of `feat/relay-followups`). Operator `go` 2026-09-17 12:5x CST.

| step | result |
|---|---|
| §1 admission (hub, `corepack pnpm@10.32.1`, `nats-server` v2.14.3 pinned) | sha ✓ · clean tree ✓ · install --frozen-lockfile ✓ · shared build ✓ · typecheck shared/relay/peer-agent ✓ · **typecheck e2e FAIL — pre-existing on develop `565cb42` (`src/harness.ts(63,17) TS2345`), not this candidate** · test:ci shared 157 / relay 468 / peer-agent 547 ✓ · build ✓ · audit --prod ✓ · audit ✓ · diff --check ✓ |
| artifact | `packages/peer-agent/dist/index.js` sha256 `b775ac614b4273dd…` — **unchanged from 0c875ed** (tsc emits per file; index.js has no diff). Per-file identity for this candidate: `dist/tools.js` `96490b9fef31a830…` (contains `renderPeerSessionLines`). |
| §2.1 backup | `~/.local/state/hangar-bridge/backups/20260917T050008Z-0c875edc…/` — sqlite `.backup` (quick_check ok, 729 rows in `message`), peers.json, installer, unit, metadata.json (previous_live `0c875ed`, candidate `fb4c156`) |
| §2.2 install | `install-relay.sh --revision fb4c156… --enable` → active; `/health` `{ok:true, build_revision: fb4c156…, version 0.4.0}`; MainPID cwd `~/projects/hangar-bridge` ✓; authenticated route probe NOT RUN (no operator curl conf); journal: presence POSTs 200 + one 429 (rate limit, expected on reconnect burst) |
| relay-side feature | `list_peers` from the hub (old dist) already shows `cuda online cockpit: 0 peers` — the handle-level "most recent non-empty" summary now surfaces the cockpit's roster instead of a peer-agent's `(connected)` (F-P3-1 relay half live) |
| §3 peers | rebuilt to `fb4c156`, `tools.js 96490b9f…`, `install-mcp.sh` dry-run + install: aimax395 (node 24) · cookys-gentoo (24) · crosshair8-hero (24; `hangar-kimi-courier` restarted, `peer.switchboard.ready`) · 7840hs (24) · twgs-revival cookys (24 via nvm alias) · twgs-revival twgs-dev + codepower (node 18: `npx pnpm` returns non-zero after a successful step — build re-run directly, artifact verified by sha + grep; `hangar-bridge-peer.service` restarted on twgs-dev) · itx WSL (build ✓; `install-mcp.sh` N/A — no jq and the courier is spawned by tunnel-client, not `~/.claude.json`; tunnel + courier killed, watchdog respawned in 14 s, readyz 200, new peer-agent pid on the new dist) |
| nonce | hub → cuda `fb4c156-8c1e4a7d` (`msg_01M2PW4WR6QQ46Q3WSK07F2GW7`, durable, 5 live) — three cuda sessions answered with the nonce in their own model turns and their `list_peers` text; cuda → hub received in the hub's model turn ✓ |
| `bin/hangar-bridge-fleet.sh status` | `twgs-revival/cookys` (the only `can-dispatch:true` principal) **FAIL gate=exact-session**: `agent-call` is not on that login's PATH and the named session `revival-cookys-claude` is not running (offline exact target). Artifact on that login verified directly (`fb4c156`, `tools.js 96490b9f…`, MCP entry installed). model-observation NOT RUN for that principal. |
| not yet | running Claude sessions keep the old peer-agent until they restart (cuda declined to restart: supervising two live ChatGPT Blender runs) — per-session `list_peers` lines appear only after that; `HANGAR_METRICS_TOKEN` still unset |

Rollback: restore `~/.local/state/hangar-bridge/backups/20260917T050008Z-0c875edc…/{hangar-bridge.sqlite,peers.json}` + `install-relay.sh --revision 0c875edc0f9a11b276aaeab7a4273e6d4a0f495c --enable`; peers: `git checkout 0c875ed && pnpm -F @hangar-bridge/peer-agent build` on each clone.
