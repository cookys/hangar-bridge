# Backlog — hangar-bridge

Deferred work, ideas, and known gaps. autopilot:next scans this file. Promote an item to a
`docs/projects/<date>-<name>/` when it grows to L-size.

## Format
`- [ ] <one-line item> — <why / context>  (size: S/L/Fix)`

## Open
- [ ] **peer-agent local_target decline rule is on `develop` but not deployed** — `inbound.ts` declines a `meta.local_target` envelope this peer is not the courier for (claude-channel: always; single-target courier: foreign names), so a misaddressed `--local` send costs no Claude session a turn (hangar gotcha `a-local-send-to-the-wrong-handle-is-a-host-broadcast`, 2026-09-19). Ships with the next `hangar:deploy-hangar-bridge` run; **post-deploy check**: re-send the incident shape (`fleet send --to cuda --local <unknown-pane>` from the hub) and confirm every cuda Claude session logs `peer.inbound.local_target_ignored` and surfaces nothing. Companion to fleet-comms plan `2026-09-19-cockpit-owns-the-pane-final-mile` P3b.  (size: S)
- [ ] **replay butler counts have no raw-row ceiling under a gated flood** — both `scanBacklog` (`routes/stream.ts`) and the `pending_after` tail loop (`routes/messages.ts`) cap *pollable/deliverable* rows at 10 000 but page through every raw row in between; a handle flooded with rows whose subject the reader does not own forces a full backlog walk per reconnect / per poll (rate-limited to 120 polls/min per token). Add a second raw-scan ceiling that reports `pending_capped` when hit. Trigger: a relay log shows stream-open or poll latency above ~1 s, or the subject ACL starts being used to gate high-volume automated senders.  (size: S)
- [ ] **replay butler: a `replay_max` reconnect at or under the threshold drains the population twice** — `routes/stream.ts` runs `scanBacklog` (no writes) and then, when nothing needs summarizing, the classic per-row drain over the same rows; correct (T1/T2) but double the SQLite reads on every reconnect that carries `replay_max`, which is every upgraded peer. Fold the scan into a single pass that emits directly when the count stays ≤ threshold. Trigger: a relay `pnpm test:ci` run or fleet-pulse shows stream-open latency on a busy reconnect, or the next change to the drain loop.  (size: S)
- [ ] **reply routing rollout step 2 — client side outside this repo** (`feat/reply-routing`, REPLY_ROUTING_SPEC.md v7 §15): dotfiles `bin/fleet` (`fleet reply`, `--all-sessions`, instance header courier/`~cli`, `fleet inbox` on `/v1/inbox`, lazy attach + one-line notice, mailbox notice) and `zsh/crew.zsh` (`agent-call attach --tmux-pane` when no channel, detach on exit §8.4); `@cookys/agent-call` (`attach --if-absent`, generation ULID, `SUPPORTED_HARNESSES`, pid-ancestry + pane-command preconditions §8.1, `send --from`, §14 `use_relay_lane` preflight); `~/.claude/CLAUDE.md` § Fleet messaging two rows (reply verb; `--instance` same host, `--to <own handle>` needs `--all-sessions`). Deliberately out of scope of the relay/peer-agent branch. **2026-09-12 partial (`fix/cli-mailbox-sender`)**: the relay now accepts `~cli` on `/v1/messages` (it never had, so the CLI could not use its own identity), and dotfiles `bin/fleet` sends as `~cli` outside a pane / without a courier and drains `GET /v1/inbox` (local cursor, `--peek`) at the top of `fleet inbox`. Still open here: `crew.zsh` attach/detach, `@cookys/agent-call` preconditions + `send --from` + `use_relay_lane`, the two CLAUDE.md rows.  (size: L)
- [ ] **reply routing rollout step 3 gate — hangar `bin/fleet-pulse.sh --health`** (§16): three per-principal columns `fleet_cli` / `agent_call` / `courier` over the existing ssh probe, `ADDRESS_RULES_GATE: PASS|FAIL` line reading `docs/evidence/address-rules-gate.json` (pins + three reply-shape entries, `courier_pane` twice); flip `HANGAR_RELAY_ADDRESS_RULES=on` only on PASS. The manifest skeleton ships in this repo; the pulse column and the evidence entries do not.  (size: L)
- [ ] **reply routing follow-ups noted by the review panel, not adopted in the branch** — `reply_route` retention beyond `expires_at` for durable rows (§3.4 follow-up); optional index on `reply_route(thread_root)`; `fetchMailboxSince` is handle-scoped, not team-scoped (single-team relay today); JCS serializer does not reject lone UTF-16 surrogates (digest only feeds retry-mismatch detection).  (size: S)
- [ ] **`packages/peer-agent/src/stream-client.test.ts` "closes the previous connection before reconnecting after a delivery error" flakes under `pnpm -r test:ci` load** — failed 4× on 2026-09-05 across untouched-package runs, passes every time in isolation; a scheduler-contention race in the reconnect-count loop. Fix the timing dependence in the test (fake timers or an explicit reconnect signal) rather than retrying CI.  (size: Fix)
- [ ] **`repo` derivation collapses local bare-remote layouts to the literal `origin`** — `roots.ts` takes the last path segment of `remote.origin.url`, which is right for `…/owner/name.git` but wrong for the common local-bare layout `~/projects/<project>/origin.git`, where the project name sits in the second-to-last segment. Observed live on the cuda box: a session in `~/projects/fighter/qwen3.8-27b` reports `repo: "origin"`. This stopped being cosmetic when `to_filter --repo` shipped: that session cannot be reached by `--repo fighter`, and any other box using the same layout collapses into one shared `origin` group, turning `--repo origin` into an unintended broadcast. Candidate fixes (each with tradeoffs, none validated): walk up a segment when the basename is `origin`/`.git`, or derive from `git rev-parse --show-toplevel` instead of the remote. No survey of the fleet's actual remote layouts exists yet.  (size: S)
- [ ] **sibling processing rights are unassigned** — a durable row is `poll_inbox`-visible to every session sharing the handle (by design, so nobody misses traffic), but N sessions may each answer it. One box currently runs 4 peer-agents on one handle (three Claude, one **grok** — the fleet's only non-Claude harness peer). It has not caused duplicate replies yet, and the peer that reported it was explicit about why: *"重複回覆沒發生過，不代表機制存在 — 今天兩次都是人為介入補上的."* Two candidate shapes, neither adopted: **pre-emptive** (claim the thread; needs a registration habit that the 2026-08-31 hardware-claim episode showed is hard to establish) and **assigned** (one session holds the handle's inbox role, others speak only with new information; worked that day only because an operator named the holder). Recorded without a solution.  (size: L)
- [ ] Complete the remaining real-host SSE self-loopback + cross-host (openclaw↔cookys-gentoo) operator smoke — source/in-process paths are green, but production host access is outside this closeout.  (size: L)
- [ ] NATS P5 real-fleet cutover + soak — needs fleet host/access inputs, NATS location, and an operator-approved soak window; follow the Board-gated runbook.  (size: L)
- [ ] Port or explicitly retire relay-backed cooperative claims before NATS P6 — deleting the relay currently removes `claim_asset` / `list_claims` / `release_claim` and SQLite schema-v6 claim state.  (size: L)
- [ ] Add an SSE↔NATS bridge only if seamless mixed-mode cutover is required — current cohorts are intentionally isolated; whole-fleet cutover is the supported P5 path.  (size: L)
- [ ] Complete real-fleet subject-ACL rollout — update muyan `peers.json` ownership, restart/re-seed relay, and set each peer's interest filters; requires operator/fleet access.  (size: S)
- [ ] CLAUDE.md "Windows-specific notes" section may be stale — dev now on Linux (zsh); audit & trim.  (size: S)
- [ ] Live-peer e2e for outbound permission relay — two real Claude sessions under `CLAUDE_DRIVER=cli` (CC v2.1.81+) confirming CC emits `notifications/claude/channel/permission_request` and applies the returned verdict; current coverage is unit-level only (P2.3).  (size: S)
- [ ] DispatchTracker: clear a matched correlation on task_result instead of leaving it to TTL (`inbound.ts` dispatch-matched branch) — tightens the correlation window; today a matched entry lingers until DISPATCH_REQUEST_TIMEOUT_MS.  (size: S)
- [ ] DispatchTracker persistence is a synchronous `writeFileSync` on the dispatch hot path — fine at current volume; revisit (async/batched) if dispatch throughput grows.  (size: S)
- [ ] Startup validation: when `permission_relay.enabled=true` but `self` is missing from config, the outbound relay silently fails-closed (relays to nobody). Consider a loud startup error / validation so a misconfigured relay is caught at boot instead of silently no-op'ing. (size: S)
- [ ] Define a versioned claim-key taxonomy only if consumers need enforced `repo:` / `file:` / `config:` kinds — today all valid keys are one generic asset kind and prefixes are conventions.  (size: S)
- [ ] Retain NATS presence summary/session metadata if parity requires it — current NATS `list_peers` intentionally exposes roster + TTL `online`/`last_seen` only and returns empty `summary`/`sessions`; SSE remains the full-metadata path.  (size: S)
- [ ] Add a production MCP response tool for structured `task_result` plus a two-real-Claude round-trip smoke — the wire kind/receive path exists, but receivers currently report completion through `send_to_peer` chat.  (size: L)
- [ ] Design session-addressed NATS task/result routing with transactional shared correlation state — current handle-scoped WorkQueue semantics are protected by a host-global one-process-per-handle lock.  (size: L)

## Done
- [x] 2026-09-17 — `@team` fanout no longer skips the sending HANDLE: an unqualified broadcast excludes only the sending instance (`fanout.ts`, branch feat/relay-followups), so a host paging the fleet about itself is heard by its own sibling sessions; a legacy sender with no instance keeps the whole-handle skip. Was: **`@team` fanout skips the sending handle, so the machine with the problem is the one that cannot hear its own alert** — `fanout.ts:106` does `if (handle === e.from) continue`. That is right for…
- [x] 2026-09-17 — closed as already done: `presence.ts` builds the envelope and calls `fanout.deliver` only, never `store.insert`; presence rows no longer enter the durable buffer (verified by direct read during the relay follow-ups plan review). Was: **presence heartbeats must not enter the durable message buffer** — every presence_update is an `@team` broadcast, so `presence.ts:51` INSERTs it; measure…
- [x] 2026-09-17 — closed as stale: base already mints `correlation_id`, persists a reply route and resolves a reply through the correlation-id alias (`messages.ts:505-583`, `replies.ts:239-397`); an unknown parent is a 404 `unknown_parent`, not a 400. Caught by the plan-review sol seat (gen 1) when the plan proposed re-implementing it. Was: **ephemeral messages have no working reply path** — `messages.ts:168` tells the receiver to reply via `meta.correlation_id` instead of `in_reply_to` (whic…
- [x] 2026-08-30 — Repaired legacy relay Docker packaging in the
  [exact-SHA deployment-hardening project](./projects/2026-08-30-exact-sha-deployment-hardening/README.md):
  current workspace names/runtime env, exact build revision, and explicit read-only peers roster.
